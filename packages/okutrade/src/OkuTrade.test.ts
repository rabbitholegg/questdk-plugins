import { GreaterThanOrEqual, apply } from '@rabbitholegg/questdk/filter'
import { describe, expect, test } from 'vitest'

// Replace *project* with the name of the project
describe('Given the OkuTrade plugin', () => {
  describe('When handling the swap action', () => {
    describe('should return a valid action filter', () => {
      test('with a valid swap transaction', () => {})
    })

    describe('should pass filter with valid swap transactions', () => {})

    describe('should not pass filter with invalid transactions', () => {})

    describe('a valid tokenlist should be returned for each supported network', () => {})
  })
})
